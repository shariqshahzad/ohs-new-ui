export const initialState = '';

export const userInitialState: User | null = null;

export const loadingInitialState = false;

export const pcRequestIdInitialState = 0;

export const langDir = 'ltr';

export const role: string | null = null;
